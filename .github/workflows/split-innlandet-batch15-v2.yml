name: Split Innlandet batch 15 v2

# Trigger generated split outputs.
on:
  workflow_dispatch:
  push:
    branches:
      - data/split-innlandet-batch15-places-v2

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-innlandet-batch15-places-v2
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-historie-innlandet-batch15-places.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/places/historie/innlandet/places_historie_innlandet_batch15 data/places/historie/innlandet/places_historie_innlandet_batch15_manifest.json data/places/historie/innlandet/places_historie_innlandet_batch15_index.json data/places/historie/innlandet/places_historie_innlandet_batch15_split_report.txt
          git commit -m "Generate split Historie Innlandet batch 15 place files" || exit 0
          git push
