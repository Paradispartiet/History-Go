name: TEMP Bankplassen v3 materialize v5

on:
  push:
    branches:
      - repair/bankplassen-4-v3-20260916

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate workflow registration
        run: echo bankplassen-v5-workflow-valid
