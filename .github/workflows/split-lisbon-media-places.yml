name: Split Lisbon media places

on:
  push:
    branches:
      - data/split-lisbon-media-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-media-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-media-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/media/europe/portugal/lisbon/places_lisbon_media data/places/media/europe/portugal/lisbon/places_lisbon_media_manifest.json data/places/media/europe/portugal/lisbon/places_lisbon_media_index.json data/places/media/europe/portugal/lisbon/places_lisbon_media_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon media place files"
            git push
          fi
