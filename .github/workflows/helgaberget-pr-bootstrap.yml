name: Build Helgaberget quiz PR

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: helgaberget-quiz-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build-quiz:
    if: github.head_ref == 'agent/etne-helgaberget-quiz'
    runs-on: ubuntu-latest
    steps:
      - name: Check out source branch
        uses: actions/checkout@v4
        with:
          ref: agent/etne-helgaberget-quiz
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Assemble and register quiz
        run: node .tmp/bootstrap-helgaberget.mjs assemble

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Build canonical Knowledge links
        run: npm run knowledge:canonical:write

      - name: Validate Helgaberget quiz contract
        run: node .tmp/bootstrap-helgaberget.mjs validate

      - name: Audit quiz content
        run: npm run audit:quiz-content

      - name: Audit quiz templates
        run: npm run audit:quiz-templates

      - name: Test quiz content audit
        run: npm run test:quiz-content-audit

      - name: Check canonical Knowledge data
        run: npm run knowledge:canonical:check

      - name: Commit final quiz package
        run: |
          rm -rf .tmp
          rm -f .github/workflows/helgaberget-pr-bootstrap.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Add Helgaberget quiz'
          git push origin HEAD:agent/etne-helgaberget-quiz
