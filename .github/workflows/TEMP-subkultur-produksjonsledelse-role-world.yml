name: TEMP Subkultur Produksjonsledelse Role World

on:
  push:
    branches:
      - agent/civication-subkultur-produksjonsledelse-role-world-rollout

permissions:
  contents: write

jobs:
  materialize:
    if: ${{ !contains(github.event.head_commit.message, '[materialized]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Syntax check Role World materializer
        run: node --check .github/TEMP-subkultur-produksjonsledelse-role-world-materializer.mjs
      - name: Materialize authored Role World
        run: node .github/TEMP-subkultur-produksjonsledelse-role-world-materializer.mjs
      - name: Make standing consequences beat-specific
        run: node .github/TEMP-subkultur-produksjonsledelse-role-world-uniqueness-fix.mjs
      - name: Regenerate Role World readiness
        run: node scripts/audit-civication-role-world-rollout-readiness.mjs --write
      - name: Focused Role World gate
        run: node tests/civication-subkultur-produksjonsledelse-role-world-rollout.test.js
      - name: Prerequisite compatibility
        run: node tests/civication-subkultur-produksjonsledelse-prerequisites.test.js
      - name: Subkultur career compatibility
        run: node tests/civication-subkultur-career-architecture.test.js
      - name: Career gameplay parity
        run: node scripts/audit-civication-career-gameplay.mjs --check
      - name: Role World readiness parity
        run: node scripts/audit-civication-role-world-rollout-readiness.mjs --check
      - name: Compiled scene registry parity
        run: node scripts/build-civication-scene-registry.mjs --check
      - name: Full Civication
        run: npm run test:civication
      - name: Repository diff gate
        run: git diff --check
      - name: Publish permanent Role World state
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/TEMP-subkultur-produksjonsledelse-role-world-materializer.mjs
          rm -f .github/TEMP-subkultur-produksjonsledelse-role-world-uniqueness-fix.mjs
          rm -f .github/workflows/TEMP-subkultur-produksjonsledelse-role-world.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            data/Civication/roleWorlds/subkultur/subkultur_produksjonsledelse.json \
            data/Civication/roleWorlds/index.json \
            data/Civication/roleWorldAuthoringChecklist.json \
            data/Civication/roleWorldThemeBank.json \
            data/Civication/roleWorldRolloutReadiness.json \
            reports/CIVICATION_SUBKULTUR_PRODUKSJONSLEDELSE_ROLE_WORLD_ROLLOUT_SOURCE_FIRST.md \
            reports/civication-role-world-rollout-readiness.md \
            tests/civication-subkultur-produksjonsledelse-role-world-rollout.test.js \
            .github/TEMP-subkultur-produksjonsledelse-role-world-materializer.mjs \
            .github/TEMP-subkultur-produksjonsledelse-role-world-uniqueness-fix.mjs \
            .github/workflows/TEMP-subkultur-produksjonsledelse-role-world.yml
          git diff --cached --check
          git commit -m "Complete Subkultur Produksjonsledelse Role World [materialized]"
          git push origin HEAD:${GITHUB_REF_NAME}
