name: Split Lisbon popkultur places

on:
  push:
    branches:
      - data/split-lisbon-popkultur-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-popkultur-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-popkultur-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/popkultur/europe/portugal/lisbon/places_lisbon_populaerkultur data/places/popkultur/europe/portugal/lisbon/places_lisbon_populaerkultur_manifest.json data/places/popkultur/europe/portugal/lisbon/places_lisbon_populaerkultur_index.json data/places/popkultur/europe/portugal/lisbon/places_lisbon_populaerkultur_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon popkultur place files"
            git push
          fi
