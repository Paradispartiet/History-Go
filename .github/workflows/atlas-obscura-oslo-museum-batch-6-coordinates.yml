name: Integrate Atlas Obscura Oslo museum batch 6

on:
  push:
    branches:
      - agent/atlas-obscura-oslo-museum-batch-6

permissions:
  contents: write

jobs:
  integrate:
    if: github.event.head_commit.message != 'Generate Atlas Obscura museum batch 6 data' && github.event.head_commit.message != 'Save Atlas Obscura museum batch 6 coordinates'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/atlas-obscura-oslo-museum-batch-6
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - name: Register active source
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const manifestPath = 'data/places/manifest.json';
          const source = 'places/historie/oslo/places_historie_atlas_obscura_museum_batch_06.json';
          const manifest = JSON.parse(fs.readFileSync(manifestPath, 'utf8'));
          manifest.files ||= [];
          if (!manifest.files.includes(source)) manifest.files.push(source);
          fs.writeFileSync(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`);
          NODE
      - name: Build and validate place data
        run: |
          mkdir -p reports/atlas-obscura-oslo-museum-batch-6/validation
          npm run places:index:build \
            | tee reports/atlas-obscura-oslo-museum-batch-6/validation/places-index-build.txt
          npm run places:index:check \
            | tee reports/atlas-obscura-oslo-museum-batch-6/validation/places-index-check.txt
          npm run places:coords:intake \
            | tee reports/atlas-obscura-oslo-museum-batch-6/validation/coordinate-intake.txt
          npm run places:emner:check \
            | tee reports/atlas-obscura-oslo-museum-batch-6/validation/emne-check.txt
          npm run health:places \
            | tee reports/atlas-obscura-oslo-museum-batch-6/validation/health-places.txt
      - name: Commit generated data
        if: always()
        run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add \
            data/places/manifest.json \
            data/places/places_index.json \
            reports/atlas-obscura-oslo-museum-batch-6/validation
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate Atlas Obscura museum batch 6 data"
            git push
          fi
