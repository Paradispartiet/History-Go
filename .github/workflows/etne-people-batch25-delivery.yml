name: Etne People of Places batch 25 validation

on:
  pull_request:
    paths:
      - '.github/workflows/etne-people-batch25-delivery.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.number == 2369
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: agent/etne-people-of-places-batch25
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run batch 25 regression test
        run: node tests/etne-people-of-places-batch25.test.js

      - name: Run full people checks
        run: bash scripts/check-people.sh

      - name: Audit Etne coverage and Langfoss uniqueness
        run: |
          node <<'NODE'
          const fs = require('fs');
          const path = require('path');
          const runtimePlaces = JSON.parse(fs.readFileSync('data/places/places_index.json', 'utf8'));
          const activeIds = new Set(runtimePlaces.map((place) => place.id));
          const placeManifest = JSON.parse(fs.readFileSync('data/places/manifest.json', 'utf8'));
          const etneIds = new Set();
          const etnePlaces = [];
          for (const rel of placeManifest.files) {
            const doc = JSON.parse(fs.readFileSync(path.join('data', rel), 'utf8'));
            for (const place of (Array.isArray(doc) ? doc : [doc])) {
              if (!place?.id || !activeIds.has(place.id) || String(place.kommune || '').toLowerCase() !== 'etne') continue;
              if (etneIds.has(place.id)) throw new Error(`Duplicate active Etne place: ${place.id}`);
              etneIds.add(place.id);
              etnePlaces.push(place);
            }
          }
          const peopleManifest = JSON.parse(fs.readFileSync('data/people/manifest.json', 'utf8'));
          const covered = new Set();
          const targetLinks = [];
          for (const rel of peopleManifest.files) {
            const doc = JSON.parse(fs.readFileSync(path.join('data', rel), 'utf8'));
            for (const person of (Array.isArray(doc) ? doc : [doc])) {
              const links = new Set([person.placeId, ...(person.places || [])].filter(Boolean));
              for (const id of links) if (etneIds.has(id)) covered.add(id);
              if (links.has('langfoss_etne')) targetLinks.push({ id: person.id, name: person.name, file: rel });
            }
          }
          const uncovered = etnePlaces.map((place) => place.id).filter((id) => !covered.has(id)).sort();
          const expected = [{ id: 'leif_grindheim', name: 'Leif Grindheim', file: 'people/natur/vestland/etne/people_langfoss_batch1.json' }];
          if (JSON.stringify(targetLinks) !== JSON.stringify(expected)) throw new Error(`Unexpected Langfoss links: ${JSON.stringify(targetLinks)}`);
          if (etnePlaces.length !== 81 || covered.size !== 66 || uncovered.length !== 15) {
            throw new Error(`Unexpected coverage: active=${etnePlaces.length} covered=${covered.size} uncovered=${uncovered.length}`);
          }
          console.log(JSON.stringify({ status: 'PASS', activeEtnePlaces: 81, coveredEtnePlaces: 66, uncoveredEtnePlaces: 15, targetLinks, uncovered }, null, 2));
          NODE

      - name: Run typecheck
        run: npm run typecheck

      - name: Run tools check against known main story baseline
        run: |
          set +e
          npm run tools:check 2>&1 | tee /tmp/tools-check.log
          status=${PIPESTATUS[0]}
          set -e
          if [ "$status" -eq 0 ]; then exit 0; fi
          node <<'NODE'
          const fs = require('fs');
          const log = fs.readFileSync('/tmp/tools-check.log', 'utf8');
          const actual = log.split(/\r?\n/).filter((line) => line.startsWith('- Ugyldig place_id:'));
          const expected = [
            '- Ugyldig place_id: story=st_regjeringskvartalet_terror_2011 field=related_places[0] place_id=utoya file=data/stories/stories_regjeringskvartalet.json',
            '- Ugyldig place_id: story=st_mollergata_19_petter_moens_dagbok field=related_places[1] place_id=norges_hjemmefrontmuseum file=data/stories/stories_mollergata_19.json',
            '- Ugyldig place_id: story=st_mollergata_19_petter_moens_dagbok field=next_scenes[0].place_id place_id=norges_hjemmefrontmuseum file=data/stories/stories_mollergata_19.json',
            '- Ugyldig place_id: story=st_folketeateret_kino_teater_opera_musikal field=related_places[1] place_id=operaen file=data/stories/stories_folketeateret.json',
            '- Ugyldig place_id: story=st_folketeateret_kino_teater_opera_musikal field=next_scenes[0].place_id place_id=operaen file=data/stories/stories_folketeateret.json'
          ];
          if (!log.includes('Stories integrity FAILED') || JSON.stringify(actual) !== JSON.stringify(expected)) throw new Error('Known story baseline changed');
          console.log('PASS known five-story-reference baseline');
          NODE

      - name: Run diff whitespace check
        run: git diff --check
