name: Register Ostfold batch 2 category files

on:
  push:
    branches:
      - main
    paths:
      - .github/ostfold-batch2-split-trigger.txt

permissions:
  contents: write

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Split category files, reduce batch, and update manifest
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          batch_path = Path('data/places/historie/ostfold/places_historie_ostfold_batch2.json')
          batch = json.loads(batch_path.read_text(encoding='utf-8'))
          moves = [
              ('orje_sluser_haldenkanalen', 'by', Path('data/places/by/ostfold/orje_sluser_haldenkanalen.json')),
              ('askim_gummivarefabrikk', 'naeringsliv', Path('data/places/naeringsliv/ostfold/askim_gummivarefabrikk.json')),
          ]
          move_ids = {place_id for place_id, _, _ in moves}
          by_id = {place['id']: place for place in batch}
          missing = move_ids - set(by_id)
          if missing:
              raise SystemExit(f'Missing Ostfold batch 2 move IDs: {sorted(missing)}')

          for place_id, category, destination in moves:
              if destination.exists():
                  raise SystemExit(f'Destination already exists: {destination}')
              place = dict(by_id[place_id])
              place['category'] = category
              destination.parent.mkdir(parents=True, exist_ok=True)
              destination.write_text(
                  json.dumps([place], ensure_ascii=False, indent=2) + '\n',
                  encoding='utf-8',
              )

          reduced = [place for place in batch if place['id'] not in move_ids]
          expected_retained = [
              'gjellestadskipet_jellhaugen',
              'hoytorp_fort',
              'basmo_festning',
              'eidsberg_kirke',
              'rygge_kirke',
              'hvaler_kirke',
          ]
          if [place['id'] for place in reduced] != expected_retained:
              raise SystemExit(
                  f'Unexpected retained Ostfold batch 2 IDs: {[place["id"] for place in reduced]}'
              )
          if any(place.get('category') != 'historie' for place in reduced):
              raise SystemExit('Retained Ostfold batch 2 place has non-historie category')
          batch_path.write_text(
              json.dumps(reduced, ensure_ascii=False, indent=2) + '\n',
              encoding='utf-8',
          )

          manifest_path = Path('data/places/manifest.json')
          manifest = json.loads(manifest_path.read_text(encoding='utf-8'))
          files = manifest['files']
          anchor = 'places/historie/ostfold/places_historie_ostfold_batch2.json'
          additions = [
              'places/by/ostfold/orje_sluser_haldenkanalen.json',
              'places/naeringsliv/ostfold/askim_gummivarefabrikk.json',
          ]
          for item in additions:
              while item in files:
                  files.remove(item)
          insert_at = files.index(anchor) + 1
          files[insert_at:insert_at] = additions
          manifest_path.write_text(
              json.dumps(manifest, ensure_ascii=False, separators=(',', ':')),
              encoding='utf-8',
          )
          PY
      - name: Commit changes and remove one-shot files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/places/by/ostfold/orje_sluser_haldenkanalen.json
          git add data/places/naeringsliv/ostfold/askim_gummivarefabrikk.json
          git add data/places/historie/ostfold/places_historie_ostfold_batch2.json data/places/manifest.json
          git rm .github/workflows/register-ostfold-batch2-category-files.yml
          git rm .github/ostfold-batch2-split-trigger.txt
          git commit -m "Split and register Ostfold batch 2 category files"
          git push origin main
