name: Split Lisbon kunst places

on:
  push:
    branches:
      - data/split-lisbon-kunst-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-kunst-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-kunst-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/kunst/europe/portugal/lisbon/places_lisbon_kunst data/places/kunst/europe/portugal/lisbon/places_lisbon_kunst_manifest.json data/places/kunst/europe/portugal/lisbon/places_lisbon_kunst_index.json data/places/kunst/europe/portugal/lisbon/places_lisbon_kunst_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon kunst place files"
            git push
          fi
