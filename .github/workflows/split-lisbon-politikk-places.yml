name: Split Lisbon politikk places

on:
  push:
    branches:
      - data/split-lisbon-politikk-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-politikk-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-politikk-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/politikk/europe/portugal/lisbon/places_lisbon_politikk data/places/politikk/europe/portugal/lisbon/places_lisbon_politikk_manifest.json data/places/politikk/europe/portugal/lisbon/places_lisbon_politikk_index.json data/places/politikk/europe/portugal/lisbon/places_lisbon_politikk_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon politikk place files"
            git push
          fi
