name: Split Lisbon vitenskap places

on:
  push:
    branches:
      - data/split-lisbon-vitenskap-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-vitenskap-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-vitenskap-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/vitenskap/europe/portugal/lisbon/places_lisbon_vitenskap data/places/vitenskap/europe/portugal/lisbon/places_lisbon_vitenskap_manifest.json data/places/vitenskap/europe/portugal/lisbon/places_lisbon_vitenskap_index.json data/places/vitenskap/europe/portugal/lisbon/places_lisbon_vitenskap_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon vitenskap place files"
            git push
          fi
