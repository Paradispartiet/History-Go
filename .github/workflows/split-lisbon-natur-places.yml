name: Split Lisbon natur places

on:
  push:
    branches:
      - data/split-lisbon-natur-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-natur-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-natur-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/natur/europe/portugal/lisbon/places_lisbon_natur data/places/natur/europe/portugal/lisbon/places_lisbon_natur_manifest.json data/places/natur/europe/portugal/lisbon/places_lisbon_natur_index.json data/places/natur/europe/portugal/lisbon/places_lisbon_natur_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon natur place files"
            git push
          fi
