name: Split Oslo psykologi places

on:
  push:
    branches:
      - data/split-psykologi-oslo-places-20260708

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-psykologi-oslo-places-20260708
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-psykologi-oslo-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/psykologi/oslo/places_psykologi data/places/psykologi/oslo/places_psykologi_manifest.json data/places/psykologi/oslo/places_psykologi_index.json data/places/psykologi/oslo/places_psykologi_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Oslo psykologi place files"
            git push
          fi
