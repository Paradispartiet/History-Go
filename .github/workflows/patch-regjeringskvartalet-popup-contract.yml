name: Patch Regjeringskvartalet popup contract fixture

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/patch-regjeringskvartalet-popup-contract.yml"

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.number == 4674
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/regjeringskvartalet-phase-13-ui-audit
          fetch-depth: 0

      - name: Patch popup contract fixture
        run: python3 scripts/patch_regjeringskvartalet_popup_contract.py

      - name: Commit fixture correction
        run: |
          rm scripts/patch_regjeringskvartalet_popup_contract.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/regjeringskvartalet-ui-production-audit.test.mjs scripts/patch_regjeringskvartalet_popup_contract.py
          git commit -m "Merk fase 13-fixturen med popup-v2-kontrakten"
          git push origin HEAD:agent/regjeringskvartalet-phase-13-ui-audit
