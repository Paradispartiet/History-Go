name: Split Oslo subkultur places

on:
  push:
    branches:
      - data/split-subkultur-oslo-places-20260708

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-subkultur-oslo-places-20260708
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-subkultur-oslo-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/subkultur/oslo/places_subkultur data/places/subkultur/oslo/places_subkultur_manifest.json data/places/subkultur/oslo/places_subkultur_index.json data/places/subkultur/oslo/places_subkultur_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Oslo subkultur place files"
            git push
          fi
