name: Fagverk Film TV legacy equivalence

on:
  pull_request:
    paths:
      - 'data/fag/TV_og_Film/**'
      - 'data/fagverk/film_tv/**'
      - 'data/fagverk/fagverk_portal.json'
      - 'reports/fagverk/film-tv-holistic-completion-v1-audit.json'
      - 'merker/merker.html'
      - 'scripts/audit-fagverk-film-tv-legacy-theory.mjs'
      - 'scripts/audit-fagverk-film-tv-legacy-adjudication.mjs'
      - 'scripts/audit-fagverk-badge-equivalence.mjs'
      - 'tests/fagverk-film-tv-legacy-theory-audit.test.mjs'
      - 'tests/fagverk-film-tv-legacy-theory-adjudication.test.mjs'
      - 'tests/fagverk-badge-equivalence.test.mjs'
      - '.github/workflows/fagverk-film-tv-legacy-equivalence.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: Verify Film TV legacy equivalence and retired route
        run: |
          node --check scripts/audit-fagverk-film-tv-legacy-theory.mjs
          node scripts/audit-fagverk-film-tv-legacy-theory.mjs
          node --test tests/fagverk-film-tv-legacy-theory-audit.test.mjs
          node --check scripts/audit-fagverk-film-tv-legacy-adjudication.mjs
          node scripts/audit-fagverk-film-tv-legacy-adjudication.mjs
          node --test tests/fagverk-film-tv-legacy-theory-adjudication.test.mjs
          node --test tests/fagverk-badge-equivalence.test.mjs
