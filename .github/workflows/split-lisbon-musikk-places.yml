name: Split Lisbon musikk places

on:
  push:
    branches:
      - data/split-lisbon-musikk-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-musikk-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-musikk-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/musikk/europe/portugal/lisbon/places_lisbon_musikk data/places/musikk/europe/portugal/lisbon/places_lisbon_musikk_manifest.json data/places/musikk/europe/portugal/lisbon/places_lisbon_musikk_index.json data/places/musikk/europe/portugal/lisbon/places_lisbon_musikk_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon musikk place files"
            git push
          fi
