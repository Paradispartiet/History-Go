name: Split Oslo vitenskap places

on:
  push:
    branches:
      - data/split-vitenskap-oslo-places-20260708

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-vitenskap-oslo-places-20260708
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-vitenskap-oslo-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/vitenskap/oslo/places_vitenskap data/places/vitenskap/oslo/places_vitenskap_manifest.json data/places/vitenskap/oslo/places_vitenskap_index.json data/places/vitenskap/oslo/places_vitenskap_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Oslo vitenskap place files"
            git push
          fi
