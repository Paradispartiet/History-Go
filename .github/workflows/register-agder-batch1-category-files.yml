name: Register Agder batch 1 category files

on:
  push:
    branches:
      - main
    paths:
      - .github/agder-batch1-split-trigger.txt

permissions:
  contents: write

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Split category file, reduce batch, and update manifest
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          batch_path = Path('data/places/historie/agder/places_historie_agder_batch1.json')
          batch = json.loads(batch_path.read_text(encoding='utf-8'))
          ids = [place['id'] for place in batch]
          if len(ids) != len(set(ids)):
              raise SystemExit('Duplicate IDs in Agder batch 1')

          moves = [
              ('kristiansand_domkirke_byhistorie', 'by', Path('data/places/by/agder/kristiansand_domkirke_byhistorie.json')),
          ]
          move_ids = {place_id for place_id, _, _ in moves}
          by_id = {place['id']: place for place in batch}
          missing = move_ids - set(by_id)
          if missing:
              raise SystemExit(f'Missing Agder batch 1 move IDs: {sorted(missing)}')

          for place_id, category, destination in moves:
              if destination.exists():
                  raise SystemExit(f'Destination already exists: {destination}')
              place = dict(by_id[place_id])
              place['category'] = category
              destination.parent.mkdir(parents=True, exist_ok=True)
              destination.write_text(
                  json.dumps([place], ensure_ascii=False, indent=2) + '\n',
                  encoding='utf-8',
              )

          reduced = [place for place in batch if place['id'] not in move_ids]
          expected_retained = [
              'christiansholm_festning_kristiansand',
              'bykle_gamle_kyrkje',
              'setesdalsmuseet_rysstad',
          ]
          if [place['id'] for place in reduced] != expected_retained:
              raise SystemExit(
                  f'Unexpected retained Agder batch 1 IDs: {[place["id"] for place in reduced]}'
              )
          if any(place.get('category') != 'historie' for place in reduced):
              raise SystemExit('Retained Agder batch 1 place has non-historie category')
          batch_path.write_text(
              json.dumps(reduced, ensure_ascii=False, indent=2) + '\n',
              encoding='utf-8',
          )

          manifest_path = Path('data/places/manifest.json')
          manifest = json.loads(manifest_path.read_text(encoding='utf-8'))
          files = manifest['files']
          anchor = 'places/historie/agder/places_historie_agder_batch1.json'
          additions = [
              'places/by/agder/kristiansand_domkirke_byhistorie.json',
          ]
          if anchor not in files:
              raise SystemExit(f'Manifest anchor missing: {anchor}')
          for item in additions:
              while item in files:
                  files.remove(item)
          insert_at = files.index(anchor) + 1
          files[insert_at:insert_at] = additions
          if len(files) != len(set(files)):
              raise SystemExit('Duplicate paths in places manifest after Agder batch 1 update')
          manifest_path.write_text(
              json.dumps(manifest, ensure_ascii=False, separators=(',', ':')),
              encoding='utf-8',
          )
          PY
      - name: Commit changes and remove one-shot files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/places/by/agder/kristiansand_domkirke_byhistorie.json
          git add data/places/historie/agder/places_historie_agder_batch1.json data/places/manifest.json
          git rm .github/workflows/register-agder-batch1-category-files.yml
          git rm .github/agder-batch1-split-trigger.txt
          git commit -m "Split and register Agder batch 1 category file"
          git push origin main
