name: TEMP By theory integrity probe

on:
  push:
    branches:
      - agent/fagverk-by-theory-integrity-proof-20260820
    paths:
      - 'tools/probe-by-theory-integrity.mjs'
      - 'tests/by-theory-integrity-probe-smoke.test.mjs'
      - '.github/workflows/temp-by-theory-integrity-probe.yml'
      - 'data/fag/by/**'
      - 'data/fagverk/by/**'
      - 'data/fagverk/fagverk_registry.json'
      - 'scripts/audit-fagverk-by-complete.mjs'
  pull_request:
    paths:
      - 'tools/probe-by-theory-integrity.mjs'
      - 'tests/by-theory-integrity-probe-smoke.test.mjs'
      - '.github/workflows/temp-by-theory-integrity-probe.yml'
      - 'data/fag/by/**'
      - 'data/fagverk/by/**'
      - 'data/fagverk/fagverk_registry.json'
      - 'scripts/audit-fagverk-by-complete.mjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check probe syntax
        run: node --check tools/probe-by-theory-integrity.mjs
      - name: Run probe smoke test
        run: node --test tests/by-theory-integrity-probe-smoke.test.mjs
      - name: Run read-only By strict-proof probe
        run: node tools/probe-by-theory-integrity.mjs | tee /tmp/by-theory-integrity-probe.json
      - name: Upload probe result
        uses: actions/upload-artifact@v4
        with:
          name: by-theory-integrity-probe
          path: /tmp/by-theory-integrity-probe.json
