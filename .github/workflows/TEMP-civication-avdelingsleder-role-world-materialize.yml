name: TEMP Materialize Avdelingsleder Role World
# Reconciled against current main before gated materialization.

on:
  push:
    branches:
      - agent/civication-naeringsliv-avdelingsleder-role-world-rollout
    paths:
      - scripts/TEMP_materialize_avdelingsleder_role_world.mjs
      - tests/civication-naeringsliv-avdelingsleder-role-world-rollout.test.js
      - .github/workflows/TEMP-civication-avdelingsleder-role-world-materialize.yml

permissions:
  contents: write

jobs:
  materialize-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/civication-naeringsliv-avdelingsleder-role-world-rollout
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Materialize permanent authored data
        run: node scripts/TEMP_materialize_avdelingsleder_role_world.mjs
      - name: Normalize strict authority and standing bindings
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const types = ['job','people','story','knowledge','micro','conflict','event','followup','consequence'];
          const prefix = 'naeringsliv_avdelingsleder_realism_';
          const catalogPath = type => `data/Civication/mailFamilies/naeringsliv/${type}/avdelingsleder_${type}.json`;

          const consequencePath = catalogPath('consequence');
          const catalog = JSON.parse(fs.readFileSync(consequencePath, 'utf8'));
          const mail = catalog.families.flatMap(family => family.mails || []).find(item => item.id === 'naeringsliv_avdelingsleder_realism_capacity_case_close_001');
          if (!mail) throw new Error('Missing Avdelingsleder close scene after materialization');

          const candidate = mail.choices?.find(choice => choice.id === 'B');
          if (candidate?.authority_action?.action_id !== 'approve_budget_exception' || candidate.authority_action.intent !== 'approve') {
            throw new Error('Unexpected authority-action source shape; refusing blind rewrite');
          }
          candidate.authority_action.intent = 'execute';

          const authority = mail.authority_context;
          if (!authority || JSON.stringify(authority.escalation_paths) !== JSON.stringify(['inger_overordnet_leder','oyvind_okonomicontroller'])) {
            throw new Error('Unexpected escalation-path source shape; refusing blind rewrite');
          }
          const capacityRule = authority.authority_rules?.find(rule => rule.action_id === 'escalate_capacity_conflict');
          if (!capacityRule || capacityRule.authority !== 'direct' || Object.prototype.hasOwnProperty.call(capacityRule, 'escalation_id')) {
            throw new Error('Unexpected capacity escalation rule; refusing blind rewrite');
          }
          const escalationId = 'avdelingsleder_capacity_conflict_to_region';
          capacityRule.escalation_id = escalationId;
          authority.escalation_paths = [{
            escalation_id: escalationId,
            action_id: 'escalate_capacity_conflict',
            target_actor_id: 'inger_overordnet_leder',
            escalation_object_id: 'avdelingsleder_capacity_conflict_escalation_001'
          }];
          fs.writeFileSync(consequencePath, JSON.stringify(catalog, null, 2) + '\n');

          let extendedStandingReasons = 0;
          for (const type of types) {
            const file = catalogPath(type);
            const doc = JSON.parse(fs.readFileSync(file, 'utf8'));
            let changed = false;
            for (const item of doc.families.flatMap(family => family.mails || [])) {
              if (!item.id.startsWith(prefix)) continue;
              for (const choice of item.choices || []) {
                for (const op of choice.effects?.social_standing_ops || []) {
                  if (typeof op.reason !== 'string') throw new Error(`${item.id}/${choice.id} standing reason missing`);
                  if (op.reason.length >= 70) continue;
                  op.reason = `${op.reason} Denne vurderingen gjelder dette publikummets situerte tillit og gir ingen ny formell fullmakt.`;
                  extendedStandingReasons += 1;
                  changed = true;
                }
              }
            }
            if (changed) fs.writeFileSync(file, JSON.stringify(doc, null, 2) + '\n');
          }
          if (extendedStandingReasons < 1) throw new Error('Expected at least one short situated-standing reason to strengthen');
          console.log(`Strengthened ${extendedStandingReasons} situated-standing reasons without changing effects or authority.`);

          const testPath = 'tests/civication-naeringsliv-avdelingsleder-role-world-rollout.test.js';
          let source = fs.readFileSync(testPath, 'utf8');
          const before = "assert.equal(close.choices[1].authority_action.intent, 'approve');";
          const after = "assert.equal(close.choices[1].authority_action.intent, 'execute');\nassert.equal(close.authority_context.escalation_paths.length, 1);\nassert.equal(close.authority_context.escalation_paths[0].target_actor_id, 'inger_overordnet_leder');\nassert.equal(close.authority_context.authority_rules.find(rule => rule.action_id === 'escalate_capacity_conflict').escalation_id, 'avdelingsleder_capacity_conflict_to_region');";
          if (!source.includes(before)) throw new Error('Expected strict-test authority assertion not found');
          source = source.replace(before, after);
          fs.writeFileSync(testPath, source);
          NODE
      - name: Regenerate scene registry
        run: node scripts/build-civication-scene-registry.mjs --write
      - name: Regenerate career gameplay audit
        run: node scripts/audit-civication-career-gameplay.mjs --write
      - name: Regenerate rollout readiness
        run: node scripts/audit-civication-role-world-rollout-readiness.mjs --write
      - name: Verify focused rollout and canonical gates
        run: |
          node tests/civication-naeringsliv-avdelingsleder-role-world-rollout.test.js
          node tests/civication-avdelingsleder-playability.test.js
          node tests/civication-role-world-contract.test.js
          node tests/civication-role-world-broad-rollout-policy.test.js
          node scripts/build-civication-scene-registry.mjs --check
          node scripts/audit-civication-career-gameplay.mjs --check
          node scripts/audit-civication-role-world-rollout-readiness.mjs --check
      - name: Run full Civication suite
        run: npm run test:civication
      - name: Remove temporary materialization surface
        run: |
          rm scripts/TEMP_materialize_avdelingsleder_role_world.mjs
          rm .github/workflows/TEMP-civication-avdelingsleder-role-world-materialize.yml
      - name: Commit exact verified result
        run: |
          git config user.name "Civication rollout agent"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Complete Næringsliv Avdelingsleder Role World rollout"
          git push origin HEAD:agent/civication-naeringsliv-avdelingsleder-role-world-rollout
