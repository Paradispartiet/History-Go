name: Temporary Dramatiker canonical sync
on:
  push:
    branches:
      - civication/litteratur-dramatiker-role-world
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: civication/litteratur-dramatiker-role-world
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Synchronize Dramatiker canonical Role World contracts
        run: node scripts/tmp-sync-dramatiker-role-world.mjs
      - name: Remove temporary sync machinery and commit generated state
        run: |
          rm -f scripts/tmp-sync-dramatiker-role-world.mjs .github/workflows/tmp-dramatiker-sync.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Synchronize Dramatiker canonical Role World state"
          git push origin HEAD:civication/litteratur-dramatiker-role-world
