name: Split Oslo popkultur places

on:
  push:
    branches:
      - data/split-popkultur-oslo-places-20260708

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-popkultur-oslo-places-20260708
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-popkultur-oslo-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/popkultur/oslo/places_oslo_populaerkultur data/places/popkultur/oslo/places_oslo_populaerkultur_manifest.json data/places/popkultur/oslo/places_oslo_populaerkultur_index.json data/places/popkultur/oslo/places_oslo_populaerkultur_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Oslo popkultur place files"
            git push
          fi
