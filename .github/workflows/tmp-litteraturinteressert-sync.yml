name: Temporary Litteraturinteressert canonical sync
on:
  push:
    branches:
      - civication/litteratur-litteraturinteressert-role-world
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: civication/litteratur-litteraturinteressert-role-world
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Generate and verify canonical Litteraturinteressert package
        run: node scripts/audit-civication-litteraturinteressert-sync.mjs
      - name: Remove temporary sync machinery and commit canonical state
        run: |
          rm -f scripts/audit-civication-litteraturinteressert-sync.mjs .github/workflows/tmp-litteraturinteressert-sync.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Civication: materialize Litteraturinteressert Role World"
          git push origin HEAD:civication/litteratur-litteraturinteressert-role-world
