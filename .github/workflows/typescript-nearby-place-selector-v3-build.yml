name: Build Nearby place selector v3 batch

on:
  pull_request:
    paths:
      - ".github/workflows/typescript-nearby-place-selector-v3-build.yml"
      - ".github/scripts/apply_nearby_place_selector_v3.py"
      - "js/ui/nearbyPlaceSelector.ts"

permissions:
  contents: write

jobs:
  build-nearby-place-selector-v3:
    if: github.event.pull_request.head.ref == 'agent/typescript-nearby-place-selector-v3'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Apply selector integration on post-left-panel main
        run: python3 .github/scripts/apply_nearby_place_selector_v3.py
      - name: Verify browser TypeScript and generate bundle
        run: |
          npm run typecheck:web
          npm run build:web
          git diff --check
      - name: Commit integration and generated runtime
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            build/build-web.mjs \
            js/app.js \
            js/ui/lists.js \
            schemas/app-globals.d.ts \
            dist/web/nearbyPlaceSelector.js
          if git diff --cached --quiet; then
            echo "No integration changes to commit"
            exit 0
          fi
          git commit -m "Extract Nearby place selection to TypeScript"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
