name: Split Oslo naering places

on:
  push:
    branches:
      - data/split-naering-oslo-places-20260708

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-naering-oslo-places-20260708
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-naering-oslo-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/naeringsliv/oslo/places_naeringsliv data/places/naeringsliv/oslo/places_naeringsliv_manifest.json data/places/naeringsliv/oslo/places_naeringsliv_index.json data/places/naeringsliv/oslo/places_naeringsliv_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Oslo naering place files"
            git push
          fi
