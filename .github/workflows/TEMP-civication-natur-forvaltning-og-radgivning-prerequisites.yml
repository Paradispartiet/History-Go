name: TEMP Civication Natur Forvaltning og rådgivning prerequisites

on:
  push:
    branches:
      - agent/civication-natur-forvaltning-og-radgivning-prerequisites
  workflow_dispatch:

concurrency:
  group: temp-civication-natur-forvaltning-og-radgivning-prerequisites
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  materialize-and-verify:
    if: >-
      !contains(github.event.head_commit.message, '[prereq-diag-skip]') &&
      !contains(github.event.head_commit.message, 'Complete Natur Forvaltning og rådgivning prerequisites')
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          ref: agent/civication-natur-forvaltning-og-radgivning-prerequisites
          fetch-depth: 0

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Syntax-check TEMP materializer and focused test
        run: |
          node --check scripts/TEMP_materialize_natur_forvaltning_og_radgivning_prerequisites.mjs
          node --check tests/civication-natur-forvaltning-og-radgivning-prerequisites.test.js

      - name: Materialize prerequisite candidate
        id: materialize
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node scripts/TEMP_materialize_natur_forvaltning_og_radgivning_prerequisites.mjs 2>&1 | tee /tmp/natur-forvaltning-og-radgivning-prerequisites-materializer.log

      - name: Persist materializer diagnostic fail-closed
        if: steps.materialize.outcome != 'success'
        shell: bash
        run: |
          set -euo pipefail
          cp /tmp/natur-forvaltning-og-radgivning-prerequisites-materializer.log /tmp/materializer.log
          git reset --hard HEAD
          git clean -fd
          mkdir -p reports/TEMP
          cp /tmp/materializer.log reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-materializer.log
          git add reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-materializer.log
          git diff --cached --check
          git commit -m "Record Natur Forvaltning og rådgivning prerequisite materializer diagnostic [prereq-diag-skip]"
          git push origin HEAD:agent/civication-natur-forvaltning-og-radgivning-prerequisites
          exit 1

      - name: Build generated prerequisite surfaces
        id: build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            echo '=== Role pack audit ==='
            node scripts/audit-civication-role-packs.mjs
            echo '=== Career gameplay audit ==='
            node scripts/audit-civication-career-gameplay.mjs --write
            echo '=== Role World readiness audit ==='
            node scripts/audit-civication-role-world-rollout-readiness.mjs --write
          } 2>&1 | tee /tmp/natur-forvaltning-og-radgivning-prerequisites-build.log

      - name: Persist generated-surface diagnostic fail-closed
        if: steps.build.outcome != 'success'
        shell: bash
        run: |
          set -euo pipefail
          cp /tmp/natur-forvaltning-og-radgivning-prerequisites-build.log /tmp/build.log
          git reset --hard HEAD
          git clean -fd
          mkdir -p reports/TEMP
          cp /tmp/build.log reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-build.log
          git add reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-build.log
          git diff --cached --check
          git commit -m "Record Natur Forvaltning og rådgivning prerequisite build diagnostic [prereq-diag-skip]"
          git push origin HEAD:agent/civication-natur-forvaltning-og-radgivning-prerequisites
          exit 1

      - name: Verify complete prerequisite candidate
        id: verify
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            echo '=== Focused Natur Forvaltning og rådgivning prerequisites ==='
            node tests/civication-natur-forvaltning-og-radgivning-prerequisites.test.js
            echo '=== Career gameplay matrix ==='
            node tests/civication-career-gameplay-matrix.test.js
            echo '=== Role World readiness ==='
            node tests/civication-role-world-rollout-readiness.test.js
            echo '=== Scene Registry parity ==='
            node scripts/build-civication-scene-registry.mjs --check
            echo '=== Scenario People parity ==='
            node --experimental-strip-types scripts/build-civication-scenario-people-index.mts --check
            echo '=== Scenario People test ==='
            node --test tests/civication-scenario-people.test.mjs
            echo '=== Full Civication suite ==='
            node tests/run-civication-tests.mjs
            echo '=== Job learning profiles ==='
            npm run audit:job-learning-profiles
            echo '=== Job knowledge requirements ==='
            npm run audit:job-knowledge-requirements
          } 2>&1 | tee /tmp/natur-forvaltning-og-radgivning-prerequisites-verification.log

      - name: Persist verification diagnostic fail-closed
        if: steps.verify.outcome != 'success'
        shell: bash
        run: |
          set -euo pipefail
          tail -n 500 /tmp/natur-forvaltning-og-radgivning-prerequisites-verification.log > /tmp/verification-tail.log
          git reset --hard HEAD
          git clean -fd
          mkdir -p reports/TEMP
          cp /tmp/verification-tail.log reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-verification.log
          git add reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-verification.log
          git diff --cached --check
          git commit -m "Record Natur Forvaltning og rådgivning prerequisite verification diagnostic [prereq-diag-skip]"
          git push origin HEAD:agent/civication-natur-forvaltning-og-radgivning-prerequisites
          exit 1

      - name: Repository diff hygiene
        if: steps.materialize.outcome == 'success' && steps.build.outcome == 'success' && steps.verify.outcome == 'success'
        run: git diff --check

      - name: Commit verified permanent prerequisites
        if: steps.materialize.outcome == 'success' && steps.build.outcome == 'success' && steps.verify.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          rm -f scripts/TEMP_materialize_natur_forvaltning_og_radgivning_prerequisites.mjs
          rm -f .github/workflows/TEMP-civication-natur-forvaltning-og-radgivning-prerequisites.yml
          rm -f reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-materializer.log
          rm -f reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-build.log
          rm -f reports/TEMP/natur-forvaltning-og-radgivning-prerequisites-verification.log
          rmdir reports/TEMP 2>/dev/null || true
          git diff --check
          git add -A
          if git diff --cached --quiet; then
            echo 'No permanent changes to commit.' >&2
            exit 1
          fi
          git diff --cached --check
          git status --short
          git commit -m "Complete Natur Forvaltning og rådgivning prerequisites"
          git push origin HEAD:agent/civication-natur-forvaltning-og-radgivning-prerequisites
