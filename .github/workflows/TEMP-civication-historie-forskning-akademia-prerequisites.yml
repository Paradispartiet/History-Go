name: TEMP Materialize Historie Forskning og akademia prerequisites

on:
  push:
    branches:
      - agent/civication-historie-forskning-akademia-prerequisites
  workflow_dispatch:

concurrency:
  group: civication-historie-forskning-akademia-prerequisites-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  materialize-and-verify:
    if: ${{ !contains(github.event.head_commit.message, 'Complete Historie Forskning og akademia prerequisites') }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/civication-historie-forskning-akademia-prerequisites
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Materialize Historie Forskning og akademia foundation
        run: node scripts/TEMP_materialize_historie_forskning_akademia_prerequisites.mjs
      - name: Clarify History Go research knowledge boundary
        run: node scripts/TEMP_fix_historie_forskning_knowledge_boundary.mjs
      - name: Differentiate research mail decisions
        run: node scripts/TEMP_fix_historie_forskning_choice_uniqueness.mjs
      - name: Regenerate Role Pack index
        run: node scripts/audit-civication-role-packs.mjs
      - name: Regenerate compiled Scene Registry
        run: node scripts/build-civication-scene-registry.mjs --write
      - name: Regenerate Career Gameplay audit
        run: node scripts/audit-civication-career-gameplay.mjs --write
      - name: Regenerate rollout readiness
        run: node scripts/audit-civication-role-world-rollout-readiness.mjs --write
      - name: Verify focused research foundation
        run: node tests/civication-historie-forskning-akademia-prerequisites.test.js
      - name: Verify Career Gameplay and readiness
        run: |
          node tests/civication-career-gameplay-matrix.test.js
          node tests/civication-role-world-rollout-readiness.test.js
      - name: Verify Role Pack depth and Scene Registry
        run: |
          node tests/civication-role-pack-depth.test.js
          node scripts/build-civication-scene-registry.mjs --check
      - name: Verify Scenario People state and invariants
        run: |
          node --experimental-strip-types scripts/build-civication-scenario-people-index.mts
          node --experimental-strip-types scripts/build-civication-scenario-people-index.mts --check
          node --test tests/civication-scenario-people.test.mjs
      - name: Run full Civication suite
        run: |
          set +e
          node tests/run-civication-tests.mjs > /tmp/civication-core.log 2>&1
          core_status=$?
          set -e
          if [ "$core_status" -ne 0 ]; then
            echo '=== Civication core failure summary ==='
            tail -n 120 /tmp/civication-core.log
            exit "$core_status"
          fi
          npm run audit:job-learning-profiles
          npm run audit:job-knowledge-requirements
          tail -n 30 /tmp/civication-core.log
      - name: Remove temporary prerequisite surfaces
        run: rm scripts/TEMP_materialize_historie_forskning_akademia_prerequisites.mjs scripts/TEMP_fix_historie_forskning_knowledge_boundary.mjs scripts/TEMP_fix_historie_forskning_choice_uniqueness.mjs .github/workflows/TEMP-civication-historie-forskning-akademia-prerequisites.yml
      - name: Commit exact verified foundation
        run: |
          git diff --check
          git config user.name "Civication rollout agent"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && { echo "No permanent changes produced"; exit 1; }
          git diff --cached --check
          git commit -m "Complete Historie Forskning og akademia prerequisites"
          git push origin HEAD:agent/civication-historie-forskning-akademia-prerequisites
