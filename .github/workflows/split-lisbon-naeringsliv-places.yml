name: Split Lisbon naeringsliv places

on:
  push:
    branches:
      - data/split-lisbon-naeringsliv-places-20260709

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/split-lisbon-naeringsliv-places-20260709
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/split-lisbon-naeringsliv-places.mjs
      - run: |
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add data/places/naeringsliv/europe/portugal/lisbon/places_lisbon_naeringsliv data/places/naeringsliv/europe/portugal/lisbon/places_lisbon_naeringsliv_manifest.json data/places/naeringsliv/europe/portugal/lisbon/places_lisbon_naeringsliv_index.json data/places/naeringsliv/europe/portugal/lisbon/places_lisbon_naeringsliv_split_report.txt
          if git diff --cached --quiet; then
            echo "No generated file changes."
          else
            git commit -m "Generate split Lisbon naeringsliv place files"
            git push
          fi
