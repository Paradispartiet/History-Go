name: Litteratur PR 5805 derived regeneration once

on:
  push:
    branches:
      - agent/litteratur-pr5805-recompose-staging-20260911

permissions:
  contents: write

concurrency:
  group: litteratur-pr5805-derived-regeneration
  cancel-in-progress: true

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/litteratur-pr5805-recompose-staging-20260911
          fetch-depth: 1
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Verify Litteratur local gates
        run: |
          node tools/audit-litteratur-theory-integrity.mjs
          node tools/audit-litteratur-expanded-quality.mjs
      - name: Regenerate global theory integrity report
        run: |
          node scripts/audit-fagverk-theory-integrity.mjs --write-report --no-check-report
          node scripts/audit-fagverk-theory-integrity.mjs
      - name: Regenerate whole-architecture release outputs
        run: |
          node scripts/materialize-natur-final-registry.mjs
          node scripts/build-fagverk-release-manifest.mjs
          node scripts/build-fagverk-release-manifest.mjs --check
          node --test tests/fagverk-release-manifest.test.mjs
      - name: Validate generated scope
        run: |
          python - <<'PY'
          import subprocess
          allowed = {
            'reports/fagverk/fagverk-theory-integrity-audit.json',
            'data/fagverk/fagverk_registry.json',
            'data/fagverk/subject_status.json',
            'data/fagverk/fagverk_release.json',
          }
          changed = set(filter(None, subprocess.check_output(['git','diff','--name-only'], text=True).splitlines()))
          print('Generated changes:')
          for path in sorted(changed): print(path)
          unexpected = sorted(changed - allowed)
          if unexpected:
            raise SystemExit('Unexpected generated paths: ' + ', '.join(unexpected))
          required = {
            'data/fagverk/fagverk_release.json',
          }
          missing = sorted(required - changed)
          if missing:
            raise SystemExit('Expected stale outputs did not regenerate: ' + ', '.join(missing))
          PY
      - name: Commit generated outputs and remove one-shot workflow
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reports/fagverk/fagverk-theory-integrity-audit.json \
            data/fagverk/fagverk_registry.json \
            data/fagverk/subject_status.json \
            data/fagverk/fagverk_release.json
          git rm .github/workflows/litteratur-pr5805-derived-regen-once.yml
          git commit -m 'Regenerate deterministic shared Fagverk outputs for PR 5805'
          git push origin HEAD:agent/litteratur-pr5805-recompose-staging-20260911
