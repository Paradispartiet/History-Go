name: Temporary Essayist canonical sync
on:
  push:
    branches:
      - civication/litteratur-essayist-role-world
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: civication/litteratur-essayist-role-world
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Synchronize Essayist canonical Role World contracts
        run: node scripts/tmp-sync-essayist-role-world.mjs
      - name: Remove temporary sync machinery and commit generated state
        run: |
          rm -f scripts/tmp-sync-essayist-role-world.mjs .github/workflows/tmp-essayist-sync.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Synchronize Essayist canonical Role World state"
          git push origin HEAD:civication/litteratur-essayist-role-world
