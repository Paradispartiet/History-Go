name: Temporary Frilansjournalist authored depth
on:
  push:
    branches:
      - civication/media-frilansjournalist-role-world
permissions:
  contents: write
jobs:
  author:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: civication/media-frilansjournalist-role-world
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Regenerate life-position readiness
        run: node scripts/audit-civication-life-position-role-world-readiness.mjs --write
      - name: Verify Frilansjournalist readiness
        run: node tests/civication-media-frilansjournalist-life-position-readiness.test.js
      - name: Verify Næringsliv Frilanser remains isolated
        run: node tests/civication-naeringsliv-frilanser-life-position-readiness.test.js
      - name: Commit authored Frilansjournalist state
        run: |
          git add \
            data/Civication/lifePositionRoleWorldReadiness.json \
            reports/civication-life-position-role-world-readiness.md
          if git diff --cached --quiet; then
            echo 'Frilansjournalist authored-depth state already synchronized.'
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m 'Synchronize Frilansjournalist authored-depth state'
          git push origin HEAD:civication/media-frilansjournalist-role-world
