#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'); const FILE='data/fag/litteratur/sprak_lingvistikk/morphology_word_structure_inflection_derivation_source_claim_brief_v1.json'; const REPORT='reports/fagverk/sprak-lingvistikk-morphology-word-structure-inflection-derivation-source-brief-v1-audit.json'; const read=f=>JSON.parse(fs.readFileSync(path.join(ROOT,f),'utf8')); const write=(f,v)=>{const p=path.join(ROOT,f);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,`${JSON.stringify(v,null,2)}\n`);}; const assert=(c,m)=>{if(!c)throw new Error(m);};
export function audit(){const b=read(FILE);assert(b.status==='source_first_ready_not_materialized','Morfologi skal være source-first');assert(b.domain?.ordinal===4&&b.domain?.id==='morfologi_ordstruktur_boyning_avledning','Feil felt 4');const ids=new Set(b.sources.map(x=>x.id)),claims=b.topic_briefs.flatMap(x=>x.planned_claims||[]);assert(b.sources.length===13&&ids.size===13,'13 kilder kreves');assert(b.sources.every(x=>/^https:\/\//u.test(x.url)&&x.retrieval_status==='verified_2026-08-31'),'Kilder må være inspectable/verifisert');assert(b.topic_briefs.length===8&&claims.length===32&&new Set(claims.map(x=>x.id)).size===32,'8 emner / 32 claims kreves');assert(claims.every(x=>x.source_ids?.length>=2&&x.source_ids.every(id=>ids.has(id))),'Claims trenger >=2 kilder');assert(b.decision_scenarios?.length===6&&b.decision_scenarios.every(x=>x.source_ids?.length>=2&&x.source_ids.every(id=>ids.has(id))),'6 case kreves');const z=b.topic_briefs.map(x=>x.boundary||'').join(' ').toLowerCase();assert(/ortografisk ord/u.test(z)&&/morfologisk ord/u.test(z),'Ordgrense mangler');assert(/segmentering/u.test(z)&&/hypotese/u.test(z),'Segmenteringsgrense mangler');assert(/gradient|teoriavhengig/u.test(z),'Infleksjon/derivasjon-grense mangler');assert(/fusion/u.test(z)&&/exponence/u.test(z)&&/synthesis/u.test(z),'Typologidimensjoner mangler');assert(/paradigm-gap|defektivitet/u.test(z),'Paradigme-gap mangler');assert(/produktivitet/u.test(z)&&/frekvens/u.test(z),'Produktivitetsgrense mangler');assert(/gloss/u.test(z)&&/rådata/u.test(z),'Gloss/rådata-grense mangler');const q={correctness_and_evidence:5,morphological_method_and_segmentation:5,paradigms_and_wordformation:5,crosslinguistic_traceability:5,assessment_readiness:5,interfaces_and_uncertainty:5};const r={schema:'history_go_sprak_lingvistikk_morphology_source_brief_audit_v1',version:'1.0.0',updated_at:'2026-08-31',status:'pass_source_first_ready_not_materialized',counts:{sources:13,topicBriefs:8,plannedClaims:32,decisionScenarios:6,plannedAssessments:8},gates:{ownership:true,inspectable_sources:true,multi_source_claims:true,word_boundary:true,segmentation:true,inflection_derivation:true,typological_dimensions:true,paradigm_gap:true,productivity:true,gloss_rawdata:true},six_part_quality_review:{...q,total:30},next_gate:'materialize_morphology_word_structure_inflection_derivation_fulltext'};write(REPORT,r);return r;}
try{const r=audit();console.log(`Språk & lingvistikk felt 4 Morfologi source-first OK: ${r.counts.sources} kilder, ${r.counts.topicBriefs} emner, ${r.counts.plannedClaims} claims, ${r.counts.decisionScenarios} case.`);}catch(e){console.error(`Språk & lingvistikk felt 4 Morfologi source-first FEIL: ${e.message}`);process.exitCode=1;}
