#!/usr/bin/env node
import fs from 'node:fs'; import path from 'node:path'; import { fileURLToPath } from 'node:url'; import { isDeepStrictEqual } from 'node:util';
import { projectSociologyMilestone } from './lib/sosiologi-antropologi-progress.mjs';
const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..'); const CHAPTER_ID = 'sted-by-migrasjon-og-transnasjonalitet'; const DIR = `data/fagverk/politikk/sosiologi_antropologi/${CHAPTER_ID}`;
const P = { chapter: `${DIR}.json`, brief: `${DIR}/brief.json`, claims: `${DIR}/claims.json`, assessment: `${DIR}/assessment.json`, sourceBrief: 'data/fag/politikk/sosiologi_antropologi/place_urbanity_migration_transnationality_source_claim_brief_v1.json', production: 'data/fag/politikk/sosiologi_antropologi/production_registry_v1.json', category: 'data/categories/category_contract.json', report: 'reports/fagverk/sosiologi-antropologi-place-urbanity-migration-transnationality-fulltext-v1-audit.json' };
const read = (file) => JSON.parse(fs.readFileSync(path.join(ROOT, file), 'utf8')); const assert = (condition, message) => { if (!condition) throw new Error(message); }; const wordCount = (text) => text.trim().split(/\s+/u).filter(Boolean).length;
function expectedReport(sourceBrief) { return { schema: 'history_go_sosiologi_antropologi_place_urbanity_migration_transnationality_fulltext_audit_v1', version: '1.0.0', updated_at: '2026-08-29', status: 'pass', conclusion: 'place_urbanity_migration_transnationality_fulltext_materialized_subcategory_still_expansion_planned', subject_id: 'politikk', canonical_subcategory_id: 'sosiologi_antropologi', chapter_id: CHAPTER_ID,
  counts: { domainsMaterialized: 9, targetDomains: 12, modules: 4, sections: 8, paragraphs: 32, verifiedClaims: 32, inspectableSources: 13, assessmentQuestions: 8, teachingScenarios: sourceBrief.decision_scenarios.length, nextSourceBriefDomains: 1 },
  gates: { definitionAndBackground: true, namedTheoriesAndResearchers: true, findingsMethodsAndLimits: true, realDisagreement: true, teachingScenarios: true, plannedClaimsResolvedOneToOne: true, paragraphClaimTraceReciprocalAndComplete: true, everySourceInspectableAndUsed: true, relationalPlaceSpaceAndScaleBoundaries: true, urbanitySegregationAndNeighborhoodBoundaries: true, migrationDefinitionStockAndFlowBoundaries: true, migrationDriverLegalStatusAndProtectionBoundaries: true, transnationalMethodAndSpatialEthicsBoundaries: true, chapterRegisteredInSubcategoryExactlyOnce: true, categoryStatusStillExpansionPlanned: true }, six_part_quality_review: { correctness_and_evidence: 5, coverage_and_completion: 5, disciplinary_editorial_quality: 5, technical_integrity: 5, safety_and_responsibility: 5, maintainability_and_auditability: 4, total: 29, maximum: 30, note: 'Felt 9 er fulltekstmaterialisert med eksplisitte skiller mellom stedlig fordeling, flyttehendelse, person, rettslig kategori, mekanisme og transnasjonal forbindelse; underkategorien er fortsatt uferdig med 9/12 felt.' } }; }
export function audit() {
  const chapter = read(P.chapter), brief = read(P.brief), claimFile = read(P.claims), assessment = read(P.assessment), sourceBrief = read(P.sourceBrief), production = read(P.production), category = read(P.category); const modules = chapter.moduleFiles.map(read), sections = modules.flatMap((module) => module.sections), paragraphs = sections.flatMap((section) => section.paragraphs), traces = sections.flatMap((section) => section.paragraphClaimIds); const planned = sourceBrief.topic_briefs.flatMap((topic) => topic.planned_claims), plannedIds = planned.map((claim) => claim.id), claims = new Map(claimFile.claims.map((claim) => [claim.id, claim])), sourceIds = new Set(claimFile.sources.map((source) => source.id));
  projectSociologyMilestone(production, category, 9);
  assert(chapter.subject_id === 'politikk' && chapter.canonical_subcategory_id === 'sosiologi_antropologi' && chapter.domain_id === 'sted_by_migrasjon_transnasjonalitet', 'Kapittelet har feil canonicalt eierskap'); assert(chapter.editorialStatus === 'chapter_ready' && chapter.claimTraceRequired === true && chapter.sourceFirst === true, 'Fulltekststatus eller claimtrace mangler'); assert(modules.length === 4 && sections.length === 8 && paragraphs.length === 32 && traces.length === 32, 'Fulltekststruktur skal være 4/8/32/32'); assert(paragraphs.every((text) => wordCount(text) >= 45) && new Set(paragraphs).size === 32, 'Alle fagavsnitt må være substansielle og unike');
  assert(plannedIds.length === 32 && new Set(plannedIds).size === 32 && claims.size === 32 && plannedIds.every((id) => claims.has(id)), 'Alle 32 briefclaims må løses én-til-én'); assert(traces.every((ids) => ids.length === 1 && claims.has(ids[0])) && new Set(traces.flat()).size === 32, 'Gjensidig paragraph↔claim-spor mangler'); assert([...claims.values()].every((claim) => claim.status === 'verified' && claim.source_ids.length >= 2 && claim.source_ids.every((id) => sourceIds.has(id))), 'Claims må være verifiserte og ha minst to kilder'); const used = new Set([...claims.values()].flatMap((claim) => claim.source_ids)); assert(sourceIds.size === 13 && [...sourceIds].every((id) => used.has(id)), 'Alle 13 kilder må være brukt'); assert(claimFile.sources.every((source) => source.url.startsWith('https://') && source.source_location && source.retrieval_status === 'verified_2026-08-29'), 'Alle kilder må være inspiserbare og verifisert');
  assert(assessment.questions.length === 8 && assessment.questions.every((q) => q.answer === q.options[q.answerIndex] && claims.has(q.claim_id) && q.source.length >= 2 && q.learner_typing === false), 'Vurdering må være claimbundet uten elevtyping'); assert(assessment.caseTasks.length === 6 && brief.realDisagreements.length >= 4 && brief.methodsAndLimits.length >= 9, 'Scenarier, metodegrenser eller reell uenighet mangler'); assert(Object.values(brief.safety).every((value) => value === false), 'Ansvarlighetsgrenser mangler'); assert(production.progress.materializedDomains === 9 && production.progress.totalDomains === 12 && production.progress.strictCompletionProven === false, 'Produksjonsregister skal vise nøyaktig 9/12 og strict=false'); assert(production.materialized.length === 9 && production.materialized.filter((row) => row.ordinal === 9 && row.chapter === P.chapter).length === 1, 'Felt 9 skal være registrert nøyaktig én gang'); const subcategory = category.canonicalSubcategories.politikk.find((row) => row.id === 'sosiologi_antropologi'); assert(subcategory?.status === 'expansion_planned', 'Underkategorien må forbli expansion_planned ved 9/12'); const report = expectedReport(sourceBrief); assert(isDeepStrictEqual(read(P.report), report), `${P.report} er utdatert`); const dimensions = ['correctness_and_evidence', 'coverage_and_completion', 'disciplinary_editorial_quality', 'technical_integrity', 'safety_and_responsibility', 'maintainability_and_auditability']; assert(dimensions.every((key) => report.six_part_quality_review[key] >= 4) && report.six_part_quality_review.total >= 27, 'Seksdimensjonal kvalitetsport feiler'); return report;
}
try { const report = audit(); console.log(`Sted, by, migrasjon og transnasjonalitet fulltekstaudit OK: ${report.counts.paragraphs} avsnitt, ${report.counts.verifiedClaims} claims, ${report.six_part_quality_review.total}/30.`); } catch (error) { console.error(`Sted, by, migrasjon og transnasjonalitet fulltekstaudit FEIL: ${error.message}`); process.exitCode = 1; }
