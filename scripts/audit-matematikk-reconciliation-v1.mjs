#!/usr/bin/env node
import path from 'node:path';import { fileURLToPath } from 'node:url';import { auditMatematikkStrict } from './audit-matematikk-strict-v1.mjs';export function audit(){const r=auditMatematikkStrict();if(!r.gates.strict_completion||r.counts.domains!==12)throw new Error('Matematikk reconciliation feilet');return {status:'pass',domains:12,materialized:12,sourceFirstReady:12,strictCompletionProven:true,nextDomain:null}}if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){try{const r=audit();console.log('Matematikk reconciliation OK: '+r.materialized+'/'+r.domains+' strict.')}catch(e){console.error(e.message);process.exitCode=1}}
