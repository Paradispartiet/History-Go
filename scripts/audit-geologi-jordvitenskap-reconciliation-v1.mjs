#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const r=p=>JSON.parse(fs.readFileSync(path.join(ROOT,p),'utf8'));const w=(p,v)=>fs.writeFileSync(path.join(ROOT,p),JSON.stringify(v,null,2)+'\n');const a=(c,m)=>{if(!c)throw new Error(m)};export function auditGeologiJordvitenskapReconciliation(){const p=r('data/fag/natur/geologi_jordvitenskap/production_registry_v1.json'),c=r('data/categories/category_contract.json'),s=r('data/fag/natur/geologi_jordvitenskap/source_registry_v1.json');a(p.status==='strict_completion_proven'&&p.progress.materializedDomains===12&&p.progress.strictCompletionProven===true,'production not strict');a((c.canonicalSubcategories?.natur||[]).some(x=>x.id==='geologi_jordvitenskap'&&x.status==='foundation_materialized'),'category contract');a(p.reuse.legacy_preserved===true&&p.reuse.legacy_chapters.length>=2,'legacy preservation');a(p.reuse.geografi_boundary_chapters.length>=4,'geografi boundary');const report={schema:'history_go_geologi_jordvitenskap_reconciliation_v1',verified_at:'2026-09-03',owner_subject_id:'natur',canonical_subcategory_id:'geologi_jordvitenskap',status:'strict_completion_proven_12_of_12',production_plan:{total_domains:12,materialized:12,next_domain:null,strict_completion_proven:true},reuse:{legacy_chapters:p.reuse.legacy_chapters,geografi_boundary_chapters:p.reuse.geografi_boundary_chapters,legacy_preserved:true},source_count:s.sources.length,next_gate:p.next_gate};w('reports/fagverk/geologi-jordvitenskap-reconciliation-v1.json',report);return report}if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){const x=auditGeologiJordvitenskapReconciliation();console.log('Geologi reconciliation OK: '+x.production_plan.materialized+'/12 strict.')}
