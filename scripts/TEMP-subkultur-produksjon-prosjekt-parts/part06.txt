_scope:ROLE,mail_type:type,families:[{id:familyByType[type],purpose,learning_focus:learningFocus,mails}]});}
const seqTypes=['job','people','knowledge','job','people','conflict','job','people','event','micro','job','people','followup','story','consequence','job'];
const stepGoals={job:'Oppdater prosjektlogg med sann status, baseline, budsjett/fullmakt, eier, avhengighet og neste kontroll.',people:'Bevar relasjon, rolle, arbeidsmiljø, kontrakt og myndighetsgrenser i arbeidsrelasjonen.',knowledge:'Bruk History Go til et bedre produksjonsspørsmål uten å gjøre historien til dagens profesjonelle fasit.',conflict:'Skill programambisjon, scope, teknisk realitet, økonomi og mandat før beslutning.',event:'Aktiver reserveplan, replanlegg berørte ledd og krev ny kontroll når hendelse endrer produksjonsgrunnlaget.',micro:'Bruk eksplisitt statusspråk for tilbud, godkjent, bestilt, signert, levert og akseptert.',followup:'Bruk faktisk arbeidstid, kostnad og avvik til bounded rework før neste prosjekt.',story:'Bær samme prosjektversjon gjennom en hel produksjonsdag med avvik og handoffs.',consequence:'Koble sen konsekvens til dokumentert restpunkt, ansvar og forbedret avslutningskriterium.'};
const plan={schema:'civication_mail_plan_v1',version:1,id:`${ROLE}_foundation_v1`,category:CATEGORY,role_scope:ROLE,title:'Produksjon og kulturprosjekt',description:'Seksten steg gjennom samme kulturprosjekt fra bestilling, scope og budsjett til crew, leverandører, rettigheter, gjennomføring, avvik, prosjektavslutning og sen konsekvens.',arc:{from:'Ny produsent med kreativ bestilling og relasjoner, men uten én felles sannhetskilde for scope, penger, kontrakter, mennesker, rettigheter og sikkerhet.',to:'Produsent som leverer under press gjennom sann status, eksplisitt fullmakt, reparerbar handoff og ren prosjektavslutning uten å gjøre miljøstatus til formell myndighet.',core_questions:['Hva er forespurt, godkjent, bestilt, signert, levert, testet, akseptert og lukket?','Hvem eier neste budsjett-, kontrakts-, rettighets-, bemannings- eller sikkerhetsavklaring?','Hvilke prosjektledd må gjenåpnes når scope, leverandør, kostnad, rettighet eller sikkerhetspremiss endres?']},situated_audience_contract:{no_global_score:true,audiences:['oppdragsgiver_og_program','crew_og_tekniske_fag','okonomi_og_innkjop','leverandorer_rettighetshavere_og_artister','sikkerhet_husdrift_og_publikum'],rule:'Standing er bundet til konkret prosjektleveranse og arbeidsrelasjon; den skaper aldri global score, signaturrett, budsjettmyndighet, arbeidsgiverfullmakt, rettighetsklarering eller sikkerhetsfrigivelse.'},outcome_rules:{promoted:{completion_ratio_gte:1,score_gte:2,strikes_lte:0},fired:{stability_values:['FIRED'],strikes_gte:3,score_lte:-3},stagnated:{autonomy_delta:-10,stability:'STAGNATED',add_branch_flags:['career_stagnated','prosjekt_uten_sporbar_baseline']}},sequence:seqTypes.map((type,i)=>({step:i+1,type,phase:i<3?'intro':i<10?'advanced':'mastery',step_goal:stepGoals[type],allowed_families:[familyByType[type]],fallback_types:[]}))};
write(MODEL,roleModel);write(GRAMMAR,grammar);write(PLAN,plan);
const report=`# Subkultur / Produksjon og prosjekt — prerequisite source-first\n\nDette er **not Role World completion**. Leveransen materialiserer bare prerequisite-laget som gjør rollen trygg nok til en separat Role World-rollout.\n\n## Kilde og rollegrense\n\n- Canonical role scope: \`${CATEGORY}/${ROLE}\` / role id \`${ROLE_ID}\`.\n- Eksisterende role model/work grammar er utvidet, ikke erstattet av ny runtime.\n- Persistent work object: \`${objectId}\`.\n- Cross-role: \`candidate_when_shared_work_is_real\`; ingen delt arbeidsobjektkobling materialiseres før samme arbeid faktisk er bevist delt.\n- History Go-affordance bruker \`club_7_vika\` som historisk refleksjonsanker, ikke som fasit for budsjett, arbeidsrett, kontrakt, HMS eller rettigheter.\n\n## Materialisert prerequisite-dybde\n\n- første dag og 16-stegs mailplan\n- fire eksplisitt fiktive arbeidsaktører og fire rolle-eide arbeidsflater\n- ni mailtyper / 15 mails uten generisk fallback\n- prosjektbaseline med waiting/venting, handoff og bounded rework\n- budsjett/innkjøp, kontrakt/fullmakt, crew/arbeidsmiljø, rettigheter, leverandører, sikkerhet, avvik og ren prosjektavslutning\n- situert standing uten global score eller myndighetslekkasje\n\n## Kvalitetsport\n\nIntern prerequisite-vurdering: **29/30**. Det ene bevisst holdte punktet er cross-role-materialisering: plausibel delt produksjonsflate finnes mot produksjonsledelse/program, men kobles ikke før et faktisk delt arbeidsobjekt og distinkte myndighetslinser er bevist.\n`;
write(REPORT,report);
const test=`const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');\nconst R=path.resolve(__dirname,'..'),read=p=>JSON.parse(fs.readFileSync(path.join(R,p))),KEY='subkultur/${ROLE}',ROLE='${ROLE}',MODEL='${MODEL}',GRAMMAR='${GRAMMAR}',PLAN='${PLAN}',TYPES=${JSON.stringify(TYPES)},ACTORS=${JSON.stringify(actorIds)},PLACES=${JSON.stringify(placeIds)};\nconst g=read(GRAMMAR);assert.equal(g.persistent_work_object_contract.id,'${objectId}');assert.match(g.rhythm_contract.loop,/waiting|venting/i);assert.deepEqual(g.actor_grammar.map(x=>x.id),ACTORS);assert.deepEqual(g.place_grammar.map(x=>x.id),PLACES);assert.ok(g.knowledge_dependencies.some(x=>x.id==='history_go_subkultur_club_7_produksjonsblikk'));assert.deepEqual(g.mail_generation_contract.required_mail_types,TYPES);assert.equal(g.day_one_contract.entry,'appointment_required');for(const term of [/arbeidsrett|arbeidsgiver/i,/sikkerhet/i,/fullmakt|mandat/i,/rettighet/i])assert.match(g.authority_boundary.may_not.join(' '),term);\nassert.equal(read('data/Civication/roleModels/manifest.json').files.filter(x=>x===MODEL).length,1);const model=read(MODEL);assert.deepEqual(model.work_life.workplaces,PLACES);assert.deepEqual(model.related_people.map(x=>x.id),ACTORS);for(const[i,p]of model.related_people.entries()){assert.equal(p.fictional,true);assert.equal(p.fictional_scenario_actor,true);assert.equal(p.canonical_person_ref,null);assert.deepEqual(p.workplace_ids,[PLACES[i]]);assert.ok(p.function.length>=220);assert.ok(p.authority_relation.length>=220)}assert.ok(model.career_path.possible_promotions.length>=2);assert.ok(model.career_path.possible_exits.length>=2);assert.ok(model.required_knowledge.history_go_badges.includes('subkultur'));\nconst plan=read(PLAN);assert.equal(plan.id,ROLE+'_foundation_v1');assert.equal(plan.sequence.length,16);assert.deepEqual(plan.sequence.map(x=>x.type),['job','people','knowledge','job','people','conflict','job','people','event','micro','job','people','followup','story','consequence','job']);for(const[i,s]of plan.sequence.entries()){assert.equal(s.step,i+1);assert.deepEqual(s.fallback_types,[]);assert.equal(s.allowed_families.length,1)}for(const x of ['promoted','fired','stagnated'])assert.ok(plan.outcome_rules[x]);\nconst C={job:4,people:4,conflict:1,story:1,event:1,micro:1,followup:1,knowledge:1,consequence:1}