import test from 'node:test';import assert from 'node:assert/strict';import {auditByRegionalGlobalPhase4 as audit} from '../scripts/audit-fagverk-by-regional-global-pendling-migrasjon-sammenligning-phase4.mjs';
test('Regionalt og globalt fullføres 2/2 uten å svekke tidligere By-domener',async()=>{const {report}=await audit();assert.equal(report.subject.editorialStatus,'chapters_in_progress');assert.equal(report.subject.registeredChapterCount,16);assert.equal(report.summary.coveredEmneCount,2);assert.equal(report.summary.sourceCount,13);assert.equal(report.summary.verifiedClaimCount,18);for(const x of Object.values(report.gates))assert.equal(x,true)});
