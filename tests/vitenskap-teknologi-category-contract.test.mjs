import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const read=(p)=>fs.readFileSync(path.join(root,p),'utf8');
const readJson=(p)=>JSON.parse(read(p));
test('Vitenskap og Teknologi deler ett canonical toppmerke',()=>{const c=readJson('data/categories/category_contract.json'),i=readJson('data/badges/index.json'),b=readJson('data/badges/vitenskap.json');assert.equal(c.runtimeCategories.length,17);assert.deepEqual(c.runtimeCategories,c.fagSubjects);assert.ok(c.runtimeCategories.includes('vitenskap'));assert.ok(!c.runtimeCategories.includes('teknologi'));for(const a of ['technology','teknologi','tech','it','informasjonsteknologi'])assert.equal(c.aliases[a],'vitenskap');assert.equal(c.labels.vitenskap,'Vitenskap & teknologi');assert.equal(b.id,'vitenskap');assert.equal(b.name,'Vitenskap & teknologi');assert.ok(b.sub.includes('teknologi'));assert.ok(!i.files.includes('data/badges/teknologi.json'));});
test('den komplette Teknologi-pakken er bevart som nested spesialisering',()=>{const m=readJson('data/fag/fag_manifest.json'),r=readJson('data/quiz/regler/QUIZ_TEMPLATE_REGISTRY_V2.json'),s=m.vitenskap.specializations.teknologi;assert.equal(m.teknologi,undefined);assert.equal(s.canonicalParentSubject,'vitenskap');assert.equal(s.badgeId,'vitenskap');assert.equal(s.status,'canonical_scientific_specialization');assert.equal(s.canonicalModelVersion,'3.0');assert.deepEqual(s.universalCoverage,{status:'complete',areas:12,topics:48,methods:35,modules:12});assert.equal(s.editorialStatus,'reviewed_and_operationalized');assert.equal(s.editorialContract,'teknologi/editorial_contract_teknologi_v3.json');assert.equal(r.category_profiles.teknologi,undefined);assert.equal(r.subject_specialization_profiles.vitenskap.teknologi,'data/fag/teknologi/supersetQUIZMAL_teknologi.json');});
test('runtimekildene normaliserer legacy teknologi-id til vitenskap',()=>{const d=read('js/DomainRegistry.js'),c=read('js/core/categories.ts'),p=read('tools/placeSchemaPolicy.mts');assert.match(d,/"teknologi": "vitenskap"/);assert.doesNotMatch(d,/"vitenskap", "teknologi"/);assert.match(c,/teknologi: "vitenskap"/);assert.doesNotMatch(c,/id: "teknologi"[^\n]*runtime_domain/);assert.match(p,/LEGACY_OR_SECONDARY_PLACE_CATEGORIES = \["film", "teknologi"\]/);});
