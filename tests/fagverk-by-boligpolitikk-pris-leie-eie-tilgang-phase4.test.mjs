import test from 'node:test';import assert from 'node:assert/strict';import {auditByBoligpolitikkPhase4 as audit} from '../scripts/audit-fagverk-by-boligpolitikk-pris-leie-eie-tilgang-phase4.mjs';
test('Boligpolitikk fullføres 1/1 og alle canonicale By-domener er dekket',async()=>{const {report}=await audit();assert.equal(report.subject.editorialStatus,'chapters_in_progress');assert.equal(report.subject.registeredChapterCount,17);assert.equal(report.summary.coveredEmneCount,1);assert.equal(report.summary.sourceCount,13);assert.equal(report.summary.verifiedClaimCount,18);for(const x of Object.values(report.gates))assert.equal(x,true)});
