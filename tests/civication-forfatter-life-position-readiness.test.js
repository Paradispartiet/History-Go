#!/usr/bin/env node
'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const ROOT=path.resolve(__dirname,'..'),read=p=>JSON.parse(fs.readFileSync(path.join(ROOT,p),'utf8'));const badge=read('data/badges/litteratur.json'),audit=read('data/Civication/lifePositionRoleWorldReadiness.json'),index=read('data/Civication/roleWorlds/index.json'),streamPath='data/Civication/narratives/leisure/litteratur_forfatter.json';const tier=badge.tiers.find(x=>x.life_position&&x.life_position.id==='forfatter');assert.ok(tier);assert.equal(tier.threshold,190);assert.equal(tier.life_position.kind,'professional_practice');assert.equal(tier.life_position.employment_independent,true);const row=audit.positions.find(x=>x.key==='litteratur/forfatter');assert.ok(row);assert.equal(row.classification,'ready');assert.equal(row.role_world_status,'role_world_complete');assert.equal(row.role_world_path,'data/Civication/roleWorlds/litteratur/litteratur_forfatter.json');assert.equal(row.authored_depth.exact_source_ref_count,1);assert.equal(row.authored_depth.max_narrative_depth,14);assert.deepEqual(row.evidence.exact_source_refs,[streamPath]);assert.ok(!audit.queue.some(x=>x.key===row.key));const indexed=index.roles.find(x=>x.life_position_key==='litteratur/forfatter');assert.ok(indexed);assert.equal(indexed.role_scope,'litteratur_forfatter');assert.equal(audit.first_ready,null);assert.equal(audit.summary.pending_ready_positions,0);console.log('Forfatter readiness gate ok');
