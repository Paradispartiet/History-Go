import test from 'node:test';import assert from 'node:assert/strict';import {auditBiologiStrict} from '../scripts/audit-biologi-strict-v1.mjs';test('Biologi strict 12/12 er materialisert og 30/30',()=>{const r=auditBiologiStrict({writeReport:false});assert.equal(r.status,'strict_completion_proven_12_of_12');assert.equal(r.counts.domains,12);assert.equal(r.counts.claims,384);assert.equal(r.counts.paragraphs,384);assert.equal(r.quality_score.total,30);});
