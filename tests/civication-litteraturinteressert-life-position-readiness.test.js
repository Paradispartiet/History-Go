#!/usr/bin/env node
'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const ROOT=path.resolve(__dirname,'..'),read=p=>JSON.parse(fs.readFileSync(path.join(ROOT,p),'utf8'));const badge=read('data/badges/litteratur.json'),audit=read('data/Civication/lifePositionRoleWorldReadiness.json'),index=read('data/Civication/roleWorlds/index.json'),streamPath='data/Civication/narratives/leisure/litteratur_litteraturinteressert.json';const tier=badge.tiers.find(x=>x.life_position&&x.life_position.id==='litteraturinteressert');assert.ok(tier);assert.equal(tier.threshold,15);assert.equal(tier.life_position.kind,'cultural_interest_status');assert.equal(tier.life_position.employment_independent,true);const row=audit.positions.find(x=>x.key==='litteratur/litteraturinteressert');assert.ok(row);assert.equal(row.classification,'ready');assert.equal(row.role_world_status,'role_world_complete');assert.equal(row.role_world_path,'data/Civication/roleWorlds/litteratur/litteratur_litteraturinteressert.json');assert.equal(row.authored_depth.exact_source_ref_count,1);assert.equal(row.authored_depth.max_narrative_depth,14);assert.deepEqual(row.evidence.exact_source_refs,[streamPath]);assert.ok(!audit.queue.some(x=>x.key===row.key));const indexed=index.roles.find(x=>x.life_position_key==='litteratur/litteraturinteressert');assert.ok(indexed);assert.equal(indexed.role_scope,'litteratur_litteraturinteressert');assert.equal(audit.first_ready,null);assert.equal(audit.summary.pending_ready_positions,0);console.log('Litteraturinteressert readiness gate ok');
