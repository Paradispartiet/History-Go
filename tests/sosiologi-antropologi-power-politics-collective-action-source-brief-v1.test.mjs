import assert from 'node:assert/strict'; import test from 'node:test'; import { generateAndAudit } from '../scripts/brief-sosiologi-antropologi-power-politics-collective-action-sources-v1.mjs';
test('makt, politikk og kollektiv handling er source-first klar som strict overlay uten å telle som materialisert', () => { const report = generateAndAudit(); assert.equal(report.status, 'pass'); assert.equal(report.counts.ordinal, 10); assert.equal(report.counts.inspectableSources, 13); assert.equal(report.counts.plannedClaims, 32); assert.equal(report.counts.domainsMaterialized, 9); assert.ok(report.six_part_quality_review.total >= 27); });
