import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const r=p=>JSON.parse(fs.readFileSync(p,'utf8'));test('Økonomi reconciliation',()=>{const x=r('reports/fagverk/okonomi-reconciliation-v1.json');assert.equal(x.status,'strict_completion_proven');assert.equal(x.legacy_preserved,true);assert.deepEqual(x.unresolved_domains,[]);for(const p of x.reused_legacy_chapters)assert.equal(fs.existsSync(p),true)});
