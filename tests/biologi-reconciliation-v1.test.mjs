import test from 'node:test';import assert from 'node:assert/strict';import {auditBiologiReconciliation} from '../scripts/audit-biologi-reconciliation-v1.mjs';test('Biologi reconciliation låser 12/12 uten neste produksjonsdomene',()=>{const r=auditBiologiReconciliation();assert.equal(r.status,'strict_completion_proven_12_of_12');assert.equal(r.production_plan.materialized,12);assert.equal(r.production_plan.next_domain,null);assert.equal(r.production_plan.strict_completion_proven,true);});
