import test from 'node:test';import assert from 'node:assert/strict';import { auditKjemiStrict } from '../scripts/audit-kjemi-strict-v1.mjs';test('Kjemi strict 12/12 er materialisert og 30/30',()=>{const r=auditKjemiStrict();assert.equal(r.counts.domains,12);assert.equal(r.counts.sources,19);assert.equal(r.counts.verifiedClaims,384);assert.equal(r.counts.paragraphs,384);assert.equal(r.gates.strict_completion,true);assert.equal(r.six_part_quality_review.total,30)});
