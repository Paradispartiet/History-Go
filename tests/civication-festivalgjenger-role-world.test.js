#!/usr/bin/env node
'use strict';const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const ROOT=path.resolve(__dirname,'..'),read=p=>JSON.parse(fs.readFileSync(path.join(ROOT,p),'utf8'));const audit=read('data/Civication/lifePositionRoleWorldReadiness.json'),stream=read('data/Civication/narratives/leisure/film_tv_festivalgjenger.json'),world=read('data/Civication/roleWorlds/film_tv/film_tv_festivalgjenger.json'),index=read('data/Civication/roleWorlds/index.json'),row=audit.positions.find(x=>x.key==='film_tv/festivalgjenger');assert.ok(row);assert.equal(stream.storylets.length,14);assert.equal(world.season.coverage.length,56);assert.equal(new Set(world.season.coverage.map(x=>x.day+'/'+x.phase)).size,56);for(const t of world.primary_threads)assert.ok(t.beat_refs.length>=5&&t.beat_refs.length<=10,t.id);assert.ok(world.primary_threads.some(x=>x.id==='audience_vs_accreditation'));assert.equal(row.role_world_status,'role_world_complete');assert.equal(row.authored_depth.max_narrative_depth,14);assert.equal(audit.summary.completed_life_position_role_worlds,27);assert.equal(audit.queue[0].key,'film_tv/kinogjenger');assert.equal(index.roles.length,112);assert.equal(index.life_position_role_world_count,27);console.log('Festivalgjenger Role World gate ok');
