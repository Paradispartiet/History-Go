import test from 'node:test';import assert from 'node:assert/strict';import {auditByDataStyringKartPlanMedvirkningAlgoritmerPhase4 as audit} from '../scripts/audit-fagverk-by-data-styring-kart-plan-medvirkning-algoritmer-phase4.mjs';
test('Data og styring fullføres 4/4 uten å svekke tidligere By-domener',async()=>{const {report}=await audit();assert.equal(report.subject.editorialStatus,'chapters_in_progress');assert.equal(report.subject.registeredChapterCount,16);assert.equal(report.chapter.id,'data-styring-kart-plan-medvirkning-algoritmer');assert.equal(report.summary.coveredEmneCount,4);assert.equal(report.summary.sourceCount,13);assert.equal(report.summary.verifiedClaimCount,18);for(const gate of Object.values(report.gates))assert.equal(gate,true);});
