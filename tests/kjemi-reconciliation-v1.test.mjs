import test from 'node:test';import assert from 'node:assert/strict';import { audit } from '../scripts/audit-kjemi-reconciliation-v1.mjs';test('Kjemi reconciliation låser 12/12 uten neste produksjonsdomene',()=>{const r=audit();assert.equal(r.domains,12);assert.equal(r.materialized,12);assert.equal(r.sourceFirstReady,12);assert.equal(r.strictCompletionProven,true);assert.equal(r.nextDomain,null)});
