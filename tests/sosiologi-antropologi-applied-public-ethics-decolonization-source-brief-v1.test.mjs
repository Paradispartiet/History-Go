import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs'; import { generate } from '../scripts/brief-sosiologi-antropologi-applied-public-ethics-decolonization-sources-v1.mjs';
test('felt-12-kildebriefen forblir source-first og kan regenereres uten å reversere strict completion', () => {
  const brief = generate(); const claims = brief.topic_briefs.flatMap((topic) => topic.planned_claims); const used = new Set(claims.flatMap((claim) => claim.source_ids)); const production = JSON.parse(fs.readFileSync('data/fag/politikk/sosiologi_antropologi/production_registry_v1.json', 'utf8')); const report = JSON.parse(fs.readFileSync('reports/fagverk/sosiologi-antropologi-applied-public-ethics-decolonization-source-brief-v1-audit.json', 'utf8'));
  assert.equal(brief.status, 'source_first_ready_not_materialized'); assert.equal(brief.domain.ordinal, 12); assert.equal(brief.sources.length, 13); assert.equal(brief.topic_briefs.length, 8); assert.equal(claims.length, 32); assert.equal(brief.decision_scenarios.length, 6); assert.ok(claims.every((claim) => claim.source_ids.length >= 2)); assert.ok(brief.sources.every((source) => source.url.startsWith('https://') && used.has(source.id))); assert.equal(brief.subcategory_upgrade_registration.registered, false); assert.equal(production.progress.materializedDomains, 12); assert.equal(production.progress.strictCompletionProven, true); assert.equal(report.status, 'pass'); assert.equal(report.six_part_quality_review.total, 29);
});
