import test from 'node:test';import assert from 'node:assert/strict';import { audit } from '../scripts/audit-juss-rettsvitenskap-reconciliation-v1.mjs';test('Juss strict 12/12',()=>{const r=audit();assert.equal(r.materialized,12);assert.equal(r.strictCompletionProven,true);assert.equal(r.nextDomain,null)});
