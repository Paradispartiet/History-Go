import test from 'node:test';import assert from 'node:assert/strict';import { auditFysikkStrict } from '../scripts/audit-fysikk-strict-v1.mjs';test('Fysikk strict 12/12 er materialisert og 30/30',()=>{const r=auditFysikkStrict();assert.equal(r.counts.domains,12);assert.equal(r.counts.verifiedClaims,384);assert.equal(r.counts.paragraphs,384);assert.equal(r.gates.strict_completion,true);assert.equal(r.six_part_quality_review.total,30)});
