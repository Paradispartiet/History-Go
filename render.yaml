services:
  - type: web
    name: history-go-aha-backend
    runtime: node
    rootDir: AHA
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
