# HG Spotmeeting PlaceCard preset demo notes

This branch keeps Spotmeeting work isolated from Civication and data manifests.

The PlaceCard preset-send demo is TEST_MODE-only and uses existing `HG_Spotmeeting` APIs.
